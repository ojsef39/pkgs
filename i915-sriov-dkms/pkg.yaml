name: i915-sriov-dkms-pkg
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  - stage: kernel-build
steps:
  - sources:
      - url: https://github.com/strongtz/i915-sriov-dkms/archive/refs/tags/2025.07.22.tar.gz
        destination: i915-sriov-dkms.tar.gz
        sha256: b25be72732521aff5b8feb643cc363f7d0f582124e5efb227dc906fa264bf4b0
        sha512: 2f630a44039a1b8d7755ff41b15e290206cfaca3e0f9c4c5280d927862f5941b63dcfc182eccc7cf5cf9962798edc48c8bec5b25b384c7bacfcf849db7e6dcc1
    env:
      ARCH: {{ if eq .ARCH "aarch64"}}arm64{{ else if eq .ARCH "x86_64" }}x86_64{{ else }}unsupported{{ end }}
    prepare:
      - |
        tar xf i915-sriov-dkms.tar.gz --strip-components=1
    build:
      - |
        # Configure the build for the current kernel
        ./configure
        # Only build i915 module, skip kvmgt since Talos doesn't have KVM
        sed -i '/obj-m += kvmgt\.o/d' Makefile
        # Build only the i915 SRIOV module
        make -C /src M=$(pwd) i915.ko
    install:
      - |
        KERNEL_RELEASE=$(cat /src/include/config/kernel.release)
        mkdir -p /rootfs/usr/lib/modules/${KERNEL_RELEASE}/
        cp /src/modules.order /rootfs/usr/lib/modules/${KERNEL_RELEASE}/
        cp /src/modules.builtin /rootfs/usr/lib/modules/${KERNEL_RELEASE}/
        cp /src/modules.builtin.modinfo /rootfs/usr/lib/modules/${KERNEL_RELEASE}/
        # Use the kernel's module installation system for proper signing
        # Create a temporary modules list with just i915
        echo "i915.ko" > modules.order
        make -C /src M=$(pwd) modules_install INSTALL_MOD_PATH=/rootfs/usr INSTALL_MOD_DIR=extras INSTALL_MOD_STRIP=1
    test:
      - |
        # Debug: see what got installed
        find /rootfs/usr/lib/modules -name "*.ko" -ls
      - |
        # Verify module exists
        ls -la /rootfs/usr/lib/modules/*/extras/i915.ko
      - |
        # Check if module is signed (allow failure for now)
        find /rootfs/usr/lib/modules -name '*.ko' -exec grep -FL '~Module signature appended~' {} \+
      - |
        fhs-validator /rootfs
finalize:
  - from: /rootfs
    to: /
